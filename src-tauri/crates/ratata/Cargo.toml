[package]
name = "ratata"
version = "0.1.2"
edition = "2021"
description = "Vendored ratata (patched for crossterm 0.27 on Windows)"

[dependencies]
ratatui = "0.26"
crossterm = "0.27"
thiserror = "1.0"

[features]
default = ["paste"]
paste = ["crossterm/bracketed-paste"]
