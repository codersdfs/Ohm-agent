[package]
name = "entropy"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
chrono = "0.4"
harness = { path = "../harness" }
tokio = { version = "1", features = ["rt", "macros"] }
