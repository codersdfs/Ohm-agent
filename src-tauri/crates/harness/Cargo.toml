[package]
name = "harness"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
regex = "1"
toml = "0.8"

[dev-dependencies]
tempfile = "3"
